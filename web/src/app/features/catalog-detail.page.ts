import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { MatButtonModule } from '@angular/material/button';
import { ApiFacade } from '../core/api.facade';
@Component({selector:'app-catalog-detail-page',imports:[RouterLink,MatButtonModule],changeDetection:ChangeDetectionStrategy.OnPush,template:`<main class="panel" tabindex="-1"><a mat-button routerLink="/catalog" [queryParams]="back()">← Wróć do wyników</a>@if(loading()){<p aria-live="polite">Ładowanie ćwiczenia…</p>}@else if(error()){<h1>Nie znaleziono ćwiczenia</h1><p role="alert">Ta wersja nie jest opublikowana lub nie istnieje.</p>}@else if(detail()){<h1>{{detail().canonicalName}}</h1><p>Opublikowana wersja v{{detail().versionNumber}}</p><h2>Instrukcja wykonania</h2><p>{{detail().instruction}}</p><h2>Wymagany sprzęt</h2><p>{{detail().requiredEquipment?.length ? detail().requiredEquipment.join(', ') : 'Brak specjalnego sprzętu'}}</p><h2>Anatomia i profil obciążenia</h2><ul>@for(a of detail().anatomyContributions;track a.code){<li><strong>{{a.displayName}}</strong> — {{a.role}}, {{a.contributionBand}}</li>}</ul><h2>Charakterystyki obciążenia</h2><ul>@for(c of detail().loadCharacteristics;track c.id){<li>{{c.characteristicType}} · {{c.movementPlane}}</li>}</ul><details><summary>Dowody</summary><ul>@for(e of detail().evidence;track e.id){<li>{{e.citation}} <small>{{e.evidenceGrade}}</small></li>}@empty{<li>Brak przypisanych cytacji.</li>}</ul></details>}</main>`})
export class CatalogDetailPage{private api=inject(ApiFacade).catalog as any;private route=inject(ActivatedRoute);detail=signal<any>(null);loading=signal(true);error=signal(false);constructor(){void this.load();}back(){return this.route.snapshot.queryParams;}async load(){try{this.detail.set(await this.api.version({versionId:this.route.snapshot.paramMap.get('versionId')}));}catch{this.error.set(true);}finally{this.loading.set(false);}}}
