import { TestBed } from '@angular/core/testing'; import { describe, expect, it } from 'vitest'; import { GoalOutcomeProgressComponent } from './goal-outcome-progress.component';
describe('GoalOutcomeProgressComponent',()=>{it('renders backend progress, target comparator and a single observation without inventing a goal lifecycle',async()=>{await TestBed.configureTestingModule({imports:[GoalOutcomeProgressComponent]}).compileComponents();const f=TestBed.createComponent(GoalOutcomeProgressComponent);f.componentInstance.outcomes=[{id:'o',metricCode:'DISTANCE',unit:'km',baseline:2,targetValue:5,targetComparator:'AT_LEAST',progress:{state:'TARGET_REACHED',baselineToTargetPercent:100}}];f.componentInstance.history=[{id:'x',outcomeId:'o',value:5,measuredAt:new Date()}];f.detectChanges();expect(f.nativeElement.textContent).toContain('TARGET_REACHED');expect(f.nativeElement.textContent).toContain('AT_LEAST');expect(f.nativeElement.querySelectorAll('circle')).toHaveLength(1);expect(f.nativeElement.textContent).not.toContain('ACHIEVED');});it('keeps multiple outcomes separate and limits no-data copy to loaded history',async()=>{await TestBed.configureTestingModule({imports:[GoalOutcomeProgressComponent]}).compileComponents();const f=TestBed.createComponent(GoalOutcomeProgressComponent);f.componentInstance.outcomes=[{id:'a',metricCode:'DISTANCE',unit:'km'},{id:'b',metricCode:'BODY_WEIGHT',unit:'kg'}];f.detectChanges();expect(f.nativeElement.querySelectorAll('article')).toHaveLength(2);expect(f.nativeElement.textContent).toContain('Brak pomiarów w wczytanej historii.');});});
describe('goal geometry',()=>{it('uses one extent for points and references',()=>{const c=new GoalOutcomeProgressComponent();const outcome={id:'o',unit:'kg',baseline:-2,targetValue:10};c.history=[{outcomeId:'o',unit:'kg',value:10,measuredAt:new Date()},{outcomeId:'o',unit:'kg',value:-2,measuredAt:new Date()}];expect(c.points(outcome).find(point=>point.value===-2)!.y).toBe(c.referenceY(-2,outcome));expect(c.points(outcome).find(point=>point.value===10)!.y).toBe(c.referenceY(10,outcome));});it('keeps one equal single point finite',()=>{const c=new GoalOutcomeProgressComponent();const outcome={id:'o',unit:'kg',baseline:4,targetValue:4};c.history=[{outcomeId:'o',unit:'kg',value:4,measuredAt:new Date()}];expect(Number.isFinite(c.points(outcome)[0].y)).toBe(true);});});
