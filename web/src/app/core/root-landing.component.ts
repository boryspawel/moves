import { Component } from '@angular/core';

/** This component is never rendered: rootLandingGuard always redirects before activation. */
@Component({ template: '' })
export class RootLandingComponent {}
